export function RestoreDialog() {
  return (
    <section className="panel-section">
      <div className="section-title">Restore</div>
      <p className="muted">Restore creates a separate project copy and leaves the active project unchanged.</p>
      <button>Restore as Copy</button>
    </section>
  );
}
